apiVersion: v1
kind: Service
metadata:
  name: prime-checker
spec:
  type: {{ .Values.service.type }}
  selector:
    app: prime-checker
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}