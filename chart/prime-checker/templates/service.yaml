apiVersion: v1
kind: Service
metadata:
  name: prime-checker
spec:
  type: ClusterIP

  selector:
    app: prime-checker

  ports:
    - name: http
      port: 80
      targetPort: 8080

    - name: metrics
      port: 9090
      targetPort: 9090