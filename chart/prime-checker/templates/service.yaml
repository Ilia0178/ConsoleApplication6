apiVersion: v1
kind: Service
metadata:
  name: {{ include "prime-checker.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: prime-checker
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}